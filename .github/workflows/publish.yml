name: Publish to GitHub Packages

on:
  push:
    tags:
      - 'v*.*.*'           # dispara em tags como v0.2.0, v1.0.0

jobs:
  publish:
    name: Publish @klini-saude/ds
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write     # necessário para publicar no GitHub Packages

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node (registry GitHub Packages)
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@klini-saude'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build library
        run: npm run build

      - name: Lint
        run: npm run lint

      - name: Set version from tag
        run: |
          TAG=${GITHUB_REF#refs/tags/v}
          CURRENT=$(node -p "require('./dist/klini-ds/package.json').version")
          if [ "$CURRENT" != "$TAG" ]; then
            npm version "$TAG" --no-git-tag-version --prefix ./dist/klini-ds
          else
            echo "Versão já é $TAG, nenhuma alteração necessária."
          fi

      - name: Publish
        run: |
          OUTPUT=$(npm publish ./dist/klini-ds --access restricted 2>&1) && echo "$OUTPUT" && exit 0
          echo "$OUTPUT"
          echo "$OUTPUT" | grep -q "previously published" && echo "Versão já publicada — ignorando." && exit 0
          exit 1
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
